mixer has rvoice
synth->channel[x].legatomode
synth->voice[x].key				# note playing
               .channel.legatomode
               .rvoice.envlfo.{volenv,modenv}.section
    fluid_rvoice_dsp_t -- nothing useful.
               .
               .
               .
==============================================================================
fluidsynth
x   bindings  - process input (not midi).
    drivers   - ...
x   gentables - conversion tables.
x   midi      - ... using input mode without midi can test things. :)
    rvoice    - ...
	src/rvoice/fluid_rvoice.c		where legato is processed? determined?
	src/sfloader/fluid_defsfont.c		where key from font is ...
	src/synth/fluid_chan.c			Default type of legato set.
						how to play poly/mono/port/legato
	src/synth/fluid_voice.c			modulators....
x   sfloader  - parse/load a sound font.
 ?  synth     - ... don't think this file applies?
 		fluid_gen.c looks like sound font stuff in here. STARTADDROFS STARTLOOPADDROFS
x   utils     - system interface
------------------------------------------------------------------------------
------------------------------------------------------------------------------
------------------------------------------------------------------------------
TO DO:
..............................................................................
A) printout instrument,  pan,  reverb, intensity in --vertical and --horizontal.
..............................................................................
B) Put in largo and ties and spaces for note tail groupings for:
    B1 - 1
       - 2
       - 3
       - 4
    B2 - 1
*   B6
------------------------------------------------------------------------------
C) Put in pizz/arco in:
    B1 - 1
       - 2
       - 3
       - 4
    B2 - 1
    B6
------------------------------------------------------------------------------
------------------------------------------------------------------------------
------------------------------------------------------------------------------
a) See if can figure out how to change fluidsynth to change note start if largo.
    If largo on, note start changes to note sustain.  That's it.
    May be nice if same 'note sample' but pitch changed, to change the pitch.
    	I was working on this, but didn't put enough checking for 'same note sample'.
------------------------------------------------------------------------------
------------------------------------------------------------------------------
------------------------------------------------------------------------------
A) To import an ABC into musescore, use the plugin "ABC Import".
   Move "X:1" to the top of the file, before any other lines. The '%' confuse it.
..............................................................................
B) To export from musescore:
    1) File->Export as MusicXML compressed (suffix .mxl).
    2) Then use program 'xml2abc my.mxl > my.abc' to convert it to abc format.
..............................................................................
