./send
	exit		./listen does exit(0);
	die		./listen does abort();
	reboot		./listen does reboot -f			without keepalive, not noticed.


	quit		./send does exit(0);
	abort		./send does abort();
	rebooting	./send does reboot -f

    Anything else is printed.



rebooting, listen starts at 12 (which is 11:30) -- lunch -- does timeout occur?
