#!/bin/bash

echo logtextmessage -v 0 SUPPORT RAID VIEWER >$2
cat $1 | while read rid type sps stat astat vid more; do if [[ "${rid//[^0123456789]*}" != "" ]]; then
        echo "logtextmessage -v 0 RID=${rid} Type=${type} STAT=${stat} AST=${astat} VID=${vid}" >>$2
fi
done 
echo quit >>$2
