This directory contains scripts that are useful when configuring/
monitoring multiple ISEs. The expect scripts pre-date the IWS stuff.

ise_configure - clean/configure one or more ISEs with a fixed number
                of volumes, host entries suitable for a DSC, etc.

ise_configure_win - A Windows version of ise_configure that uses the 
                    serial connection.  Requires ActiveTCL 
                    version 8.4.17.0 (see www.activestate.com).

ise_status - report the output of various "show <xxx>" commands
             on one or more ISEs.

ise_upgrade - performs an ISE controller firmware upgrade for one or more 
              ISEs.
