Suggested gdb commands:
------------------------------------------------------------------------------
    log -t          - dump system message buffer, without timestamps.
    bt -Flsx        - print out the stack's backtrace, with file/line/symbols.
    mod -S          - Load symbolic and debugging data for all loaded modules.
    set scroll less - set scrolling program to /usr/bin/less.
    set scroll on   - enable output scrolling.
    help            - print a list of first level commands available.
    help bt         - print help (with scrolling above) about command 'bt'.
------------------------------------------------------------------------------

