#!/bin/bash -e
A="$1"
CMD="$2"
#-----------------------------------------------------------------------------
if [ "${CMD}" = "" ]; then
    CMD='ls'
fi
# L=10000
L=1000
#-----------------------------------------------------------------------------
for (( C=0; $C<$L; C=$C+1)); do
  taskset $A ${CMD}
done
#-----------------------------------------------------------------------------
